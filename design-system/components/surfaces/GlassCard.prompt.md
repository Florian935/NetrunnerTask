Frosted-glass panel — the base surface. Add a targeted `glow` and/or HUD `brackets`.

```jsx
<GlassCard>Contenu</GlassCard>
<GlassCard glow="violet" brackets>Rappel anticipé</GlassCard>
```

Props: `glow` (`teal` | `violet` | `magenta`), `brackets`.
