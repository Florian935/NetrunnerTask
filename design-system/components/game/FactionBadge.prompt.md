Faction tag — coloured sigil chip + name with a soft glow. Used on contract cards.

```jsx
<FactionBadge name="Arasaka" color="#ff5d6c" sigil="AR" />
<FactionBadge name="Voidrunners" color="#c657ff" />
```
