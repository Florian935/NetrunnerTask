Rarity chip for cosmetics/loot — 5 neon tiers; `mythic` reads "Corrupted".

```jsx
<RarityBadge rarity="legendary" />
<RarityBadge rarity="mythic" />
```

Tiers: `common` (gray) · `rare` (blue) · `epic` (violet) · `legendary` (gold) · `mythic` (magenta). Import `RARITY` for the colour map.
