Fast task-capture field — glass, leading **+** button, ⌘K hint. Enter or + submits and clears.

```jsx
<QuickAddBar placeholder="Ajouter une tâche, une note…" onAdd={(text) => addTask(text)} />
```

Props: `placeholder`, `onAdd(text)`, `showHint`, `defaultValue`.
