Status badge for state labels.

```jsx
<Badge tone="success" dot>Complete</Badge>
<Badge tone="warning" variant="outline">In Progress</Badge>
<Badge tone="danger" dot>Overdue</Badge>
```

Tones: `success` `warning` `danger` `info` `accent` `neutral`. Variants: `soft` (default) `solid` `outline`.
