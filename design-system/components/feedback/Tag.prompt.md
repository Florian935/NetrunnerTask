Small monospace tag pill for categories/labels. Removable or clickable.

```jsx
<Tag>daily</Tag>
<Tag color="#9b6bff" onRemove={() => drop('urgent')}>urgent</Tag>
```

Props: `hash` (leading #, default true), `onRemove`, `onClick`, `color`.
