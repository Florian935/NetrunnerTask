Square icon-only button; always pass `label` for accessibility.

```jsx
<IconButton label="Settings" variant="ghost"><SettingsIcon/></IconButton>
```

Variants: `ghost` (default), `solid`, `accent`. Sizes: `sm` `md` `lg`.
