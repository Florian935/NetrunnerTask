**Tooltip** — brief on-hover hint. Wrap the trigger element; keep the label to a few words.

```jsx
<Tooltip label="Deck synced 3s ago" placement="top">
  <IconButton name="refresh-cw" />
</Tooltip>
```

Placements: top, bottom, left, right. Mono cyan text on a beveled dark chip.
