**ProgressBar** — determinate progress (downloads, uploads, health/charge meters, capacity).

```jsx
<ProgressBar value={72} label="CPU LOAD" showValue accent="magenta" />
<ProgressBar value={40} accent="gradient" />
```

Accents: cyan, magenta, mint, violet, gradient — or pass any CSS color. Neon glow tracks the fill.
