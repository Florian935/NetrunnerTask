**Breadcrumbs** — show location in a hierarchy and let users jump back up.

```jsx
<Breadcrumbs items={['Home','Analytics','Traffic']} onNavigate={go} />
```

Mono uppercase, chevron separators. The final crumb is the current page (cyan, non-clickable).
