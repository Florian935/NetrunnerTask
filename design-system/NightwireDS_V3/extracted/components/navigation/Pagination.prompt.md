**Pagination** — page through long lists/tables.

```jsx
<Pagination page={p} pageCount={12} onChange={setP} />
```

Collapses long ranges with ellipses; active page is a glowing cyan cell. `onChange` receives the new page number.
