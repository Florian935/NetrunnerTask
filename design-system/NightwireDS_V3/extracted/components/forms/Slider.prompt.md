**Slider** — pick a value in a continuous range (volume, threshold, opacity).

```jsx
<Slider value={v} onChange={setV} showValue accent="var(--magenta-500)" />
```

Glowing thumb + neon filled track. `onChange` receives a number. Recolor via `accent`.
