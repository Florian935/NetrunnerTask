# Maquettes — US-004 (Liste des contrats & complétion)

Dépose ici l'export Claude Design (liste + item de contrat + ConfirmDialog).

- Formats : images (`.png`, `.jpg`, `.svg`) ou export (`.html`, `.zip`, `.pdf`).
- Le dossier `extracted/` d'un export HTML n'est pas suivi utilement — garde le
  zip comme source (voir US-003).

Cadrage lié : `us/US-004-liste-contrats.md` (§3 Design).
